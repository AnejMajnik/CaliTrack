info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/register
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: ""
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "username": "jack",
        "password": "sparrow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
